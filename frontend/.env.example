# Optional: defaults to ws(s)://<current-host>/ws/voice
VITE_WS_URL=ws://localhost:8000/ws/voice

# Admin API key — must match ADMIN_API_KEY in backend/.env
# Required for agent management (create/edit/delete)
VITE_ADMIN_API_KEY=
